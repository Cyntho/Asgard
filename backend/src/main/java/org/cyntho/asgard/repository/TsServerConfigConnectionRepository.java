package org.cyntho.asgard.repository;

import org.cyntho.asgard.entity.TsServerConfigConnection;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TsServerConfigConnectionRepository extends JpaRepository<TsServerConfigConnection, Long> {
}
